# Let's ignore here files produced by the project tools only.
# You can ignore other files, that are not related to the project in the global gitignore:
#
# ```sh
# git config --global core.excludesFile '~/.gitignore'
# echo .os-specific-file >> ~/.gitignore
# ```
target/
tarpaulin-report.html
plans/

# Generated by stylance-cli from src/kit/*.module.scss (see Cargo.toml
# [package.metadata.stylance]). Rebuilt by the Trunk pre_build hook.
quilt-sync/ui/assets/css/kit/_modules.scss
