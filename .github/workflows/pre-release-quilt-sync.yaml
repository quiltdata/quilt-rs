name: Pre-Release QuiltSync

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        type: environment
        required: true

jobs:
  build:
    uses: ./.github/workflows/build-quilt-sync.yaml
    with:
      platforms: |
        {
          "include": [
            {"platform": "macos-15", "args": "--target aarch64-apple-darwin"},
            {"platform": "ubuntu-22.04", "args": ""}
          ]
        }
      environment: ${{ inputs.environment }}
      prerelease: true
    secrets: inherit
